# GlacierEQ Toolbelt — World-Class Map

**Ptr:** `~/GlacierEQ_Swarm/toolbelt/TOOLBELT.md`  
**Registry:** `toolbelt_registry.json` · **Doctor:** `automations/toolbelt-doctor.py`  
**Law:** measure or unknown · private-first · legal absolute lock · load skill bodies JIT

---

## Tiers

| Tier | Meaning | Action |
|------|---------|--------|
| **T0 Core** | Always on | Never disable |
| **T1 Active** | Primary work | Load on trigger |
| **T2 Support** | Domain tools | JIT |
| **T3 Optional** | Heavy / rare | Keep; don't default-load |
| **T4 Archive** | Empty/broken fixed or moved | Doctor reports |

---

## T0 — Always-on core

| Tool | Role |
|------|------|
| **token-saver** | Context discipline · pure_pointer · 100% flipper |
| **sequential-thinking** MCP | Multi-step reason |
| **ai-humanizer** MCP | Final prose naturalize (not code/logs) |
| **AGENTS.md** L0–L5 | Progressive OS |

---

## T1 — Active skills (router)

| Skill | Trigger |
|-------|---------|
| `swarm-orchestrator` | multi-step / goal / forever |
| `path-of-highest-power` | apex / colossus / max power |
| `repo-indexer` | repo map / github portfolio |
| `toolbelt` | “what tools / activate / A–Z” |
| `design` → `execute-plan` → `pr-babysit` | ship path |
| `check-work` / `review` / `code-review` | quality |
| `build-with-ai` | AI features |
| `create-skill` | new procedure |
| `help` | TUI / MCP |

---

## T2 — Domain skills

| Skill | Domain |
|-------|--------|
| `voice-memo-processor` | audio / diarize |
| `docx` / `xlsx` / `pptx` | office |
| `imagine` | image gen/edit |
| `resume-claude` | continue Claude sessions |
| `implement` | implementation loops |
| `mimo_skills/test-runner` | standardized test runs |

---

## T1 Flippers (local · prefer before LLM)

| Flipper | Job |
|---------|-----|
| `token-100pct-savings-flipper.py` | pure_pointer + ledger |
| `device-stability-flipper.py` | 8GB host health |
| `qualification-savings-flipper.py` | qual runner + savings |
| `github-ecosystem-analyzer.py` | map-anchored GH digest |
| `aeon-moc-procode-scanner.py` | pro-code MOC |
| `aworkers_orchestrator.py` | meta-workers + memory |
| `repo-public-promotion-flipper.py` | private→public gates |
| `fork-private-divergence-flipper.py` | diverged forks → private |
| **`toolbelt-doctor.py`** | health of entire belt |

Tests live in `automations/tests/` (not flipper surface).

---

## Connectors (query before spawn)

| Priority | Connector |
|----------|-----------|
| 0 | Local state · memory · Swarm |
| 1 | brainsync · sequential-thinking · github · notion |
| 2 | supabase · supermemory · vercel |
| 3 | gmail/outlook/calendar · apple-mcp |
| 4 | canva/gamma · smithery · box/gdrive · humanizer |

---

## A→Z activation (expert project)

See also prior activation path. Condensed:

**A** L0+L0b+L0c → **B** 5W1H goal → **C** map open → **D** risk matrix → **E** swarm? → **F** path? → **G** index → **H** plan/design → **I** flippers local → **J** implement → **K** review → **L** ship → **M** humanize → **N** flush/compact → **O** hire whole if job → **P** promote gates → **Q** measure → **R** skill extract if 2+ → **S–Z** forever ops / anti-patterns / stop

---

## Hire surfaces (productized)

| Surface | Path |
|---------|------|
| Unified whole | `jobapp_whole/WHOLE.md` |
| Showcase | `jobapp_showcase/SHOWCASE.md` |
| Hire pack | `jobapp_hire_package/` |
| Toolbelt | `toolbelt/TOOLBELT.md` |
| **Grok Build CLI guide** | `toolbelt/GROK_BUILD_CLI.md` |
| **AZOP orchestration** | `toolbelt/AZOP_ORCHESTRATION.md` (Kilo A–Z × Grok; not AKOS rename) |
| Config perf apply | `automations/apply_grok_config_perf.py` |

---

## Purged / fixed (this ascend)

| Item | Action |
|------|--------|
| `automations/test_*.py` | → `automations/tests/` (not flippers) |
| `mimo_skills` missing root SKILL | Router SKILL added |
| `remove-wall-of-text` empty | SKILL.md restored |
| Dead empty dirs | Doctor flags; no silent delete of large skills |
| Office skills (docx/xlsx/pptx 1MB+) | **Keep** (real value, JIT only) |

---

## Doctor

```bash
python3 ~/GlacierEQ_Swarm/automations/toolbelt-doctor.py
# → state/toolbelt_doctor_last.json
```

**Healthy belt** = all T0 present · flippers py_compile · no legal in hire surfaces · skills have SKILL.md

---

*Ascended 2026-07-13 · top of world = disciplined load order + measured local compute + zero dead flippers*
